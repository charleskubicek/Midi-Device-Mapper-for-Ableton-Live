I need to see all visible mappings on the HUD.

Currently buttons don't have any words on at all but the encoders have.

when going into a shift mode they should all be updated, currently encoder text is not updated.